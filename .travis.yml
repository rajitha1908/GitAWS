os: 
 - windows
 #- linux

language: cpp

before_install: 
- git config --global user.name rajitha1905
- git config --global user.email rajitha.siripurapu@gmail.com
- echo "${PWD}"
#- sudo apt-get install qtcreator
#- sudo apt-get install qtchooser qt5-qmake
#- sudo update-alternatives --install /usr/bin/qmake qmake /usr/lib/x86_64-linux-gnu/qt5/bin/qmake 100
#- export QT_DEBUG_PLUGINS=1
#- sudo apt-get install libqt5x11extras5
#- sudo apt-get install libxcb-render-util0 libxcb-image0 libxcb-keysyms1 libxcb-icccm4



#- sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 50
#- sudo update-alternatives --config qmake
#- sudo update-alternatives --install /usr/bin/qmake qmake /usr/lib/x86_64-linux-gnu/qt5/bin/qmake 100


#- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get install -y p7zip-full; fi

#env:
    #- QT_CI_PACKAGES=qt.qt5.5120.gcc_64,qt.qt5.5120.qtwebengine PATH="/home/travis/build/Qt/5.12.0/gcc_64/bin:${PATH}"
  
install:
- pwd
- pushd /c/Users/travis/build/ && mkdir arm-gcc && cd arm-gcc
- echo "Password:$ARTIFACTORY_USERNAME"
- curl -fL https://getcli.jfrog.io | sh
- ./jfrog rt config --url $ARTIFACTORY_URL --user $ARTIFACTORY_USERNAME $JFROG_CLI_LOG_LEVEL
- cp /c/Users/travis/build/rajitha1908/GitAWS/folder1.zip ${PWD}
- jar xf folder1.zip
- export PATH="$PATH:${PWD}"
- ls
- popd

script: 
- pwd
- if [[ "$TRAVIS_OS_NAME" == "windows" ]]; then ./build.bat; fi
- chmod +x deploy.sh
- if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then ./deploy.sh; fi

  
after_success: 
- echo "deployed"
 