os: 
 "linux"
dist:
 "focal"
language: c

before_install:
- export TRAVIS_COMMIT_MSG="$(git log --format=%B --no-merges -n 1)"
- echo "$TRAVIS_COMMIT_MSG" | grep 'Version_Bump'; export CONTAINS_NO_VERSION_BUMP=$?; true
- echo "PARSED EXPORTS | Commit Message $TRAVIS_COMMIT_MSG | Contains No version Bump $CONTAINS_NO_VERSION_BUMP"
- git config --global user.name rajitha1905
- git config --global user.email rajitha.siripurapu@gmail.com
  

  apt:
    packages:
    - libc6-i386 
    - make
    

install:
- pwd

script: 
  - chmod +x deploy.sh
  - ./deploy.sh
  
after_deploy: 
  - ls
  
