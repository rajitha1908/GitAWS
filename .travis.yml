os: 
 "linux"
dist:
 "focal"
language: python
node_js:
- "3.6"
before_install:
- git config --global user.name rajitha1905
- git config --global user.email rajitha.siripurapu@gmail.com

install:
 - pip install sh
 
jobs:
 include:
  env:
   TARGET_PLATFORM : bee_v1 || bee_v2 || bee_v3 || bee_er
   
script: 
 if [[$TARGET_PLATFORM == "bee_v1"]]; then  
   ./usr/bin/make -f Makefile.swarmbee clean
   ./usr/bin/make -f Makefile.swarmbee; 
 fi
 if [[$TARGET_PLATFORM == "bee_v2"]]; then  echo "bee v2"; fi
 if [[${TARGET_PLATFORM} == "bee_v3"]]; then  echo "bee v3"; fi
 if [${TARGET_PLATFORM} == "bee_er"]; then  echo "bee er"; fi


  
after_deploy: 
  - ls
  - cat deploy.sh
